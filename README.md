# 作业 3-A：快递物流行业财报问答知识库

> 选一个行业（快递物流，17 家上市公司），从**交易所网站/法定披露平台**下载
> 年报与半年报全文，提取文字并还原表格，切块带上「公司 / 章节 / 页码」，
> 建 **向量 + BM25 双索引**，做一个能提问、答案带出处的问答页面，
> 并用 10 道题（含跨公司全景题）逐题评测召回与作答质量。

---

## 1. 交付清单

**要提交的东西只有一个 —— `dist/作业3A_交付包_<日期>.zip`（约 4 MB，35 个文件）。**
它由 `python code/package.py` 生成，含下表全部内容；207 MB 的财报 PDF 原件
不进包（`data/manifest.json` 记录来源 URL + SHA1，`zsh run_all.sh` 可一键复现）。

| 交付项 | 位置 | 是否进交付包 |
|---|---|---|
| **正式报告（16 页 PDF）** | `output/作业3A_报告.pdf` | ✅ |
| **一页结论**（单独成册 3 页 PDF） | `output/一页结论.pdf` / `.md` | ✅ |
| 逐题评测台账 | `output/eval_table.md` | ✅ |
| 每题完整召回明细 | `output/eval_runs.json` | ✅ |
| 命中率汇总指标 | `output/eval_scores.json` | ✅ |
| 工作流程说明 | `output/工作流程说明.md` | ✅ |
| 问答页面截图（9 张） | `shots/` | ✅ |
| 代码仓库 | `code/`（7 个脚本 + Web 应用）+ 本 README | ✅ |
| 下载清单（来源 URL + SHA1） | `data/manifest.json` | ✅ |
| 切块统计 | `data/chunks/stats.json` | ✅ |
| 原始财报 PDF（34 份，207 MB） | `data/pdfs/` | ❌ 可一键复现 |
| 切块知识库 / 检索索引 / 向量模型 | `data/chunks`、`data/index`、`data/model` | ❌ 可一键复现 |

**代码仓库**：本目录即代码仓库（已 `git init`，37 个文件纳入版本管理）。
`.gitignore` 排除了 207 MB 的 PDF 原件与索引（可由 `run_all.sh` 复现），
仓库体积仅 4.2 MB。若课程要求**单独提交代码**，用 `--code-only` 出纯源码包。

**重新生成交付物：**

```bash
V=.venv/bin
$V/python code/make_report.py            # Markdown → output/*.pdf（Chrome 无头打印）
$V/python code/package.py                # 交付包 + 代码仓库包
$V/python code/package.py --code-only    # 只打纯代码仓库包（约 60 KB，19 文件）
$V/python code/package.py --date 20260924  # 指定包名日期（默认今天）
$V/python code/package.py --with-data    # 额外打含 207 MB PDF 原件的数据包
```

---

## 2. 目录结构

```
homework3/
├── code/
│   ├── 01_download.py        ① 下载年报/半年报全文（多源）
│   ├── 02_extract_chunk.py   ② 提取文字 + 表格按行列还原 + 切块带元数据
│   ├── 03_build_index.py     ③ 建向量索引 + BM25 索引
│   ├── 04_ask.py             ④ 混合检索 + 带出处的答案生成（可命令行调用）
│   ├── 05_eval.py            ⑤ 10 道题 × 4 种检索模式逐题评测（含分组召回对照）
│   ├── embedder.py           BGE-small-zh-v1.5 ONNX 编码器（含兜底方案）
│   ├── app.py                问答页面（Flask）
│   ├── make_report.py        Markdown → PDF 报告（Chrome 无头打印）
│   ├── package.py            打交付包 dist/*.zip
│   ├── eval_questions.json   评测题目与标准答案证据
│   └── requirements.txt
├── data/
│   ├── pdfs/                 34 份财报 PDF
│   ├── text/                 逐页纯文本（含 [PAGE n] 标记，便于人工核对出处）
│   ├── chunks/               chunks.jsonl + stats.json
│   ├── index/                bm25.pkl / emb.npy / index_meta.json
│   ├── model/                bge-small-zh-v1.5（ONNX，96 MB）
│   └── manifest.json         下载清单（来源、URL、大小、SHA1、公告日）
├── web/                      问答页面前端
├── output/                   正式报告 PDF、评测台账、一页结论、工作流程说明
├── shots/                    页面截图（9 张）
└── dist/                     交付包 —— 提交这个
    ├── 作业3A_交付包_<日期>.zip      4.0 MB（报告+台账+截图+代码+manifest）
    └── 作业3A_代码仓库_<日期>.zip    60 KB（纯源码，课程若单独要代码交这个）
```

> `data/model/`、`data/index/`、`data/pdfs/` 体积大且可复现，交付包里不含。

---

## 3. 环境搭建

**一条命令搞定（推荐）—— 自检 + 补装 + 装完必验：**

```bash
cd homework3          # 进入本仓库根目录
zsh code/bootstrap_deps.sh
```

脚本干三件事：① 常规依赖（有 wheel）直装；② `jieba` / `zhconv` 这类**只有
sdist** 的包，pip 装不上就自动走「下源码包 → 手工解包 → 拷进 site-packages」；
③ 收尾做**功能性自检**（zhconv 要真能把「單票收入與毛利率」转成简体才算过，
光 import 成功不算）。

手工等价命令：

```bash
python3 -m venv .venv
.venv/bin/pip install -r code/requirements.txt
```

**已知坑 1 —— pip 解包纯 sdist 包会报错：** 本机 pip 解包 `jieba`、`zhconv` 时报
`EEXIST: file already exists, mkdir .../pip-install-*/jieba_xxxx`。
这两个包是纯 Python，手工装即可：下载 tarball → `tar -xzf` → 把包目录拷进
`site-packages/`（`bootstrap_deps.sh` 已自动化这一步）。

**已知坑 2 —— jieba 缺席不该让整条流水线崩：** `03_build_index.py` 把 jieba 当
**可选依赖**处理：装了就用地道分词 + 领域词典；没装会自动降级为**内置 CJK 二元
切分 + 领域词命中**（BM25 照常可用，只是分词精度略降）。
`rank_bm25`（→ 自实现 BM25）、`sklearn`（→ 仅影响兜底向量）同样各有降级路径。
真正的**必需依赖**只有：
`pymupdf / numpy / onnxruntime / tokenizers / requests / bs4 / lxml / tqdm / flask / zhconv`。

**向量模型：** 从 hf-mirror 拉 BGE-small-zh-v1.5 的 ONNX 版（96 MB，CPU 推理，
不依赖 torch，避免装 2 GB 的深度学习栈）：

```bash
B=https://hf-mirror.com/Xenova/bge-small-zh-v1.5/resolve/main
mkdir -p data/model/bge-small-zh-v1.5/onnx && cd $_
for f in config.json tokenizer.json tokenizer_config.json special_tokens_map.json vocab.txt; do
  curl -sLO $B/$f; done
curl -sLo onnx/model.onnx $B/onnx/model.onnx
```

**一键跑全流程（幂等，可反复执行）：**

```bash
cd homework3          # 进入本仓库根目录
zsh run_all.sh
```

`run_all.sh` 的 0/7 步会自检依赖：缺了就自动调 `bootstrap_deps.sh` 补装，
补完仍缺则**直接中止**（避免白跑一遍 20 分钟的提取）。

---

## 4. 运行

一键（等价于下面 5 步 + 截图，幂等可重跑）：

```bash
zsh run_all.sh
```

分步手工跑：

```bash
V=.venv/bin
$V/python code/01_download.py        # ① 下载（约 3 分钟，207 MB）
$V/python code/02_extract_chunk.py   # ② 提取+切块（默认 3 进程并行，MAXW 可调）
$V/python code/03_build_index.py     # ③ 建索引（BGE 编码 CPU）
$V/python code/05_eval.py            # ⑤ 跑 10 题评测
$V/python code/app.py --port 8000    # 打开 http://127.0.0.1:8000

# 命令行单题检索调试
$V/python code/04_ask.py "顺丰控股2026年上半年单票收入" --mode hybrid -k 6
```

---

## 5. 数据源说明（作业要求「从交易所网站下载」）

| 市场 | 来源 | 说明 |
|---|---|---|
| 深市 | **深交所官网** `szse.cn/api/disc/announcement/annList` | 交易所官方公告接口 |
| 沪市 | **巨潮资讯网** `cninfo.com.cn` | 证监会指定的法定信息披露平台（深交所旗下深圳证券信息有限公司运营），沪深全覆盖，公告原文与上交所一致 |
| 港股 | **港交所披露易** `hkexnews.hk` | 港交所官方披露平台 |

> 上交所 `query.sse.com.cn` 接口在本机网络环境返回空结果，故沪市公司统一走巨潮
> （法定披露渠道）。脚本对每个源都做了重试与降级，全部来源 URL 记录在
> `data/manifest.json`，可逐份回溯核验。

**样本：17 家公司 × 2 类报告 = 34 份**

- 快递主干（8）：顺丰控股、圆通速递、韵达股份、申通快递（A 股）＋
  中通快递、京东物流、极兔速递（港股）
- 物流延伸（9）：东航物流、华贸物流、中国外运、中储股份、中谷物流、
  密尔克卫、传化智联、嘉友国际、铁龙物流、长久物流

> 注：原计划纳入的**德邦股份（603056）已于 2026 年被京东物流私有化退市**
> （巨潮 `delisted=true`，无在期报告），替换为长久物流（603569）。

---

## 6. 关键设计决策

### 6.1 表格按行列还原（而不是当纯文本堆着）

`page.find_tables()` 拿到表格网格后，做两步清洗：

1. **单元格内换行直接拼接**。中文财报表格的单元格换行绝大多数是排版折行：
   - `归属于上市公\n司股东的净利\n润（元）` → `归属于上市公司股东的净利润（元）`
   - `25,084,468,028\n.72` → `25,084,468,028.72`
     （不这样处理，数字会被打成 `25,084,468,028 .72`，数值问答直接崩）
2. **删掉整列为空的列**。合并单元格会让 5 列表被还原成 19 列，噪声极大。

表格块渲染成 Markdown 管道表，且在切块时**整表成块、绝不从中间切断**。

### 6.2 切块带「公司 / 章节 / 页码」

- 正文按 `第X节` 识别章节（限定出现在页面顶部、节号单调不减，以排除目录页）；
  港股报告无「第X节」体例，另用已知标题词表识别（管理层讨论与分析、财务报表附注…）
- 页眉页脚按「跨页高频重复行 + 纯页码行」剔除
- 段内按句末边界累积到约 700 字，相邻块重叠约 100 字
- 每个块携带：`公司 / 代码 / 交易所 / 细分行业 / 报告类型 / 章节 / 页码区间 / 是否表格`

### 6.3 双索引与融合

- **BM25（词法）**：jieba 分词 + 金融/快递领域自定义词
  （单票收入、中转、加盟商、扣非净利润…），擅长精确术语与数字
- **向量（语义）**：bge-small-zh-v1.5 句向量，[CLS] 池化 + L2 归一化，
  查询侧加官方检索指令；擅长「价格战怎么体现」这类没有字面重合的问题
- **融合**：RRF（Reciprocal Rank Fusion，k=60），对两路排名取调和，
  不需要调分数权重 —— 也便于在页面上直接对比三种模式的表现差异

### 6.4 答案生成走「抽取式」

不引入大模型，而是从召回块中按「查询词命中密度 + 数字命中 + 位置」挑句子，
拼成答案并**逐句挂出处**。理由：本作业的核心是评测**检索**质量，
生成式模型会把检索失败「圆过去」，掩盖问题；抽取式可复现、可审计，
每一句都能点回原始页码。代码同时留了 OpenAI 兼容接口的开关
（把 `OPENAI_BASE_URL` / `OPENAI_API_KEY` 写进 `.env` 即自动切换为 grounded 生成。
`.env` 已在 `.gitignore` 中排除，仓库里只留不含真实值的 `.env.example` 模板；
代码只读环境变量，任何 key 都不会出现在源码或提交里）。

跨公司题另做一层**按公司轮转 + 指标词优先**的答案拼装：否则同一家公司的高分句
会把 6 个位置占满（实测未处理前，Q01 的答案里圆通给的是"营业收入 386.21 亿"，
韵达却给成了"业务量 122.57 亿票"、顺丰给成了"营业成本 1,345.9 亿"）。

### 6.5 分组召回（cover 模式）—— 专治跨公司全景题

问题里点名多家公司时，单轮全局检索必然翻车：**BM25 会把「释义表」「子公司名录」
这类把公司名罗列几十遍的附录表顶到最前面**（实测圆通第八节子公司名录表逐行都是
"XX圆通速递有限公司"），Top-5 被一家公司刷屏，其它公司根本进不来。

cover 模式的做法：

1. 识别问题里点名的公司（全称 + 简称表）；
2. **剔除公司名**构建子查询（否则 BM25 继续被释义表刷屏）；
3. 对每家公司**独立检索**，组内用「指标焦点词」硬过滤，噪声块直接挡掉；
4. 各公司的结果**轮转交错合并**（而不是按公司分块），保证 Top-5 每家都有位置；
5. 不够再用全局结果补足。

两个容易踩的细节：
- 焦点词**只从指标术语表取**，绝不按 IDF 挑 —— IDF 会选中"多少""差距"
  这类财报里罕见但零信息量的疑问词；
- 同义组要**整组带出**（单票收入 / 票均收入 / 包裹单价 / 每票收入），
  否则中通那份报告里根本没有"单票收入"三个字，硬过滤会把它的候选全部清空。

### 6.6 跨市场术语同义扩写

同一个指标各家披露用词不同，查询里出现任一变体就把同组其它变体加进 BM25 词袋：

| 指标 | 顺丰（A 股） | 中通（港股） |
|---|---|---|
| 单票收入 | **票均收入** 14.4 元/票，同比 +3.3% | **包裹单价** 增加 12.0% |

不扩写的话，问"中通和顺丰的单票收入"两路都搜不到 —— 这正是"跨市场口径对齐"
这道题的考点本身。这几个跨市场术语也补进了 jieba 领域词典（57 → 62 词）并重建
BM25 索引，否则"包裹单价"会被切分成"包裹/单价"，词元层面依然对不上。

---

## 7. 评测结果

| 检索模式 | 首次评测 | 修正判卷口径后 | 加分组召回后 |
|---|---|---|---|
| BM25（仅词法） | 70% | 90% | — |
| 向量（仅语义） | 60% | 80% | — |
| hybrid（RRF 融合） | 60% | 90% | — |
| **cover（hybrid + 分组召回）** | — | — | **100%** |

> ⚠ 这是**关键词组证据命中率**（宽松上界，仅看证据词是否出现在 Top-5 块里），
> 应读作"证据全部召回"，不等价于"10 题全部答对"。逐题结论与根因分析见
> `output/一页结论.md`。

**判卷口径修正（两处，都是评测工具的缺陷而非系统缺陷）：**

1. **命中判定纳入元数据**：公司名本来就由 `companies` 过滤器强约束，
   再要求正文复述一遍会把正确召回判成失败（Q02 的 Top1 正是顺丰毛利率原文，
   只是正文里没写"顺丰"三个字）。
2. **区分「检索失败」与「语料未披露」**：Q09 问中通"市场份额"，
   全语料核查确认中通 2026 半年报未按该口径披露（该词仅出现在 2025 年报的
   风险提示里，属定性表述）。系统找不到、且**没有编造数字**，属正确行为；
   故降为 soft 组单列，不判失败。

---

## 8. 已知限制

1. **合并单元格的跨行项目名**无法完全还原 —— 项目名被拆到多行时，
   表格首列可能只保留第一行文字。
2. **扫描件/图片型表格**（少数港股文件的内嵌图片）无法提取，`find_tables` 会跳过。
3. 沪市公司依赖巨潮（上交所接口在本机网络不可达）；巨潮对**高频请求**有限流，
   脚本已加退避重试，但首次全量下载仍可能需要重跑一次。
4. 向量模型是 512 维小模型，语义召回上限有限；财务表格类问题主要靠 BM25 + 表格块。
5. **关键词组判定 ≠ 数值答对**：跨公司题的"四家公司营业收入谁最高"这类，
   证据召回与数值正确仍是两件事，需人工核验。
6. **双栏排版的句子串行**：顺丰那份报告是双栏排版，抽取式答案偶有跨栏拼接
   （需版面分析才能根治）。
7. 样本量小：每类题型只有 1 道题，单题成败会被放大成 0%/100%。

**已知坑 3 —— 受限环境里 Chrome 无头截图会 FATAL：** 在沙箱/CI/远程会话中，
Chrome 自带 sandbox 初始化失败（`Failed to initialize sandbox: Operation not
permitted`），GPU 进程退出后直接 FATAL。`code/shot.sh` 已固定加 `--no-sandbox`
与 `--disable-dev-shm-usage`，本地与受限环境都能截图。
