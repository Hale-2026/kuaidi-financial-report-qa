# ============================================================
# 环境变量模板
# ------------------------------------------------------------
# 用法：复制成 .env 再填真实值
#     cp .env.example .env
#
# .env 已被 .gitignore 排除（见 .gitignore 顶部），
# 绝不会进入版本库；代码里只读环境变量，不存任何 key。
# ============================================================

# ------------------------------------------------------------
# 说明：本项目默认不需要任何 key。
#   检索走「本地 BM25 + 本地向量模型（bge-small-zh-v1.5, ONNX, CPU）」，
#   答案走「抽取式（从召回块里挑原句 + 标注出处）」，全程离线可跑。
#
#   下面三项只在你想把答案切换成「大模型生成式」时才需要填。
#   不填时 04_ask.py 会自动走抽取式，功能完整、评测结果不受影响。
# ------------------------------------------------------------

# --- 可选：生成式答案（留空 = 用内置抽取式）---
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=
# LLM_MODEL=gpt-4o-mini
